cfm_trainable = sum(
    p.numel() for p in backbone.cfm.parameters() if p.requires_grad
)
cfm_total = sum(
    p.numel() for p in backbone.cfm.parameters()
)
runner.logger.info(
    f'[FreezeBackboneBeforeEpochHook] CFM trainable params: '
    f'{cfm_trainable:,} / {cfm_total:,}'
)
